// Type for 'entity metadata', containing information about the types of entities

type EntityMetadatum = {
  table: string,
  name: string,
  pluralName: string,
  iconName: string,
}

export default EntityMetadatum;